import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL(
    process.env.NEXT_PUBLIC_SITE_URL || "https://meta-agent.vercel.app",
  ),
  title: {
    default: "MetaAgent Studio",
    template: "%s | MetaAgent Studio",
  },
  description:
    "Build, run, and orchestrate AI agents with Groq-powered workflows.",
  openGraph: {
    title: "MetaAgent Studio",
    description:
      "Build, run, and orchestrate AI agents with Groq-powered workflows.",
    url: "/",
    siteName: "MetaAgent Studio",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
